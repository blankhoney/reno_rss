---
description: 教学模式会话：讲清功能、细节与设计原因（仅 @ teaching-session 时启用）
globs: docs/teaching-session.md
alwaysApply: false
---

# 教学模式会话（my_rss）

当前会话已通过 **`@docs/teaching-session.md`**（或编辑器中与该文件相关的上下文）启用本规则。目标是**接近一对一教学**：不只说「怎么做」，还要说明**做什么、为什么这样设计**。

## 回答结构（按需合并，避免机械化堆砌）

回答配置、命令、架构或代码时，默认尽量包含：

1. **功能**：它在栈里负责什么、输入/输出或边界是什么。
2. **细节**：关键字段/端口/网络名/文件名为何出现在这里（与上下游如何衔接）。
3. **设计原因**：为何采用这种做法；常见替代方案及**trade-off**（何时不适用）。
4. **在本仓库中的位置**：若能用简短路径或引用指向现有文件，帮助对照。

用户只要简短结论时，仍先给结论，再在段末用两三句补「为何如此」即可。

## 与全局 `learning-mode.mdc` 的关系

- 全局学习模式仍约束：**步骤拆解、风险标注、笔记更新义务**等。
- **本规则额外约束**：在解释层面默认**优先说明动机与机制**，减少「黑箱步骤列表」。

## 不要影响其他会话

- 本规则 **`alwaysApply: false`**，且 **`globs`** 仅绑定 `docs/teaching-session.md`。
- 未 @ 该文件的 Chat **不应依赖本规则**；需要教学模式时请在新消息里再次 `@docs/teaching-session.md`。
