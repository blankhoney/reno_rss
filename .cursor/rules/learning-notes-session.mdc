---
description: 追问 session：基于最新 learning-notes 作答（仅在与该文件相关时启用）
globs: docs/learning-notes.md
alwaysApply: false
---

# 追问 session（learning-notes 上下文）

用户在 **追问 session** 里学习 my_rss：用 `@docs/learning-notes.md` 引入笔记，或在本仓库中打开/聚焦该文件。

## 启用方式与本规则的关系

- 本规则通过 **`globs: docs/learning-notes.md`** 限定：仅在处理与该路径匹配的工作区上下文时附加（例如文件已 @ 或处于相关编辑场景）。
- **不要**把本规则改成 `alwaysApply: true`，否则会污染其他无关 Chat。

## 及时同步笔记内容

回答与项目进度、Task 编号、笔记里「关键概念」或「追问的问题」相关的内容前：

1. 用 **Read** 读取仓库中的 `docs/learning-notes.md`，以当前磁盘版本为准（Chat 里缓存的旧片段可能已过时）。
2. 若用户明确说「刚更新了笔记」「请看最新 Task」，必须先 Read 再答。
3. 回答中引用笔记结构时，与文件实际章节一致；若发现与用户粘贴片段冲突，以 Read 结果为准并简短说明。

## 与其他规则的分工

- 全局 **`learning-mode.mdc`** 仍负责教练式步骤与更新笔记的义务。
- **本规则只负责**：在追问场景下不把过期的 `learning-notes` 内容当成事实。
